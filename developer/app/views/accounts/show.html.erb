<%= content_for(:headline) { "Developer Dashboard" } %>

<div class="column grid_3">
	<div id="sidebar">
		<ul>
			<a href="#"><li>General</li></a>
			<li>Your Account</li>
			<li>Statistics</li>
			<li>Tutorials</li>
			<li id="helpdesk">Help Desk</li>
	</div>
</div>

<div class="column grid_9">
  <article id="dashboard">
		<h1>Welcome <%= current_account.email %></h1>
		
		<section class="note">
		The Linccer API is still in beta, you should not use it in production yet.
		<br /><br /> We appreciate your feedback at <a href="mailto:support@hoccer.com">support@hoccer.com</a>.
		</section>
		
		<section id="api_keys">
			<strong>API-Key:</strong> <%= text_field_tag(:api_key, current_account.api_key) %><br />
			<strong>Shared Secret:</strong>	<%= text_field_tag(:shared_secret, current_account.shared_secret) %>

			<p>Use this keys to authenticate your clients against the server (see in the <a href="https://github.com/hoccer">README</a> of the wrapper or in the <a href="https://developer.hoccer.com/about_api.html">REST API Documentation</a>).</p>
		</section>
		
		<section>
			<h3>Javascript</h3>
			<p>In order to use the <a href="https://github.com/hoccer/javascript">JavaScript API</a> on your website you must enter the sites' urls.</p>
		
		
		<div id="website_form">
			<h4>Your Websites:</h4>
			
			<ul>
			<% current_account.websites.each do |site| %>
				<li><%= site %> (<%= link_to 'delete', 
														 account_websites_path(site),
														:confirm => "remove #{site}?",
														:method => "delete" %>)
				</li>
			<% end %>
		
			<li><%= form_tag( account_websites_path ) do %>
				<%= text_field_tag(:website) %>
				<%= submit_tag("add") %>
			<% end %></li>
			<ul>
		</div>
		
		<p>We only allow requests from the specified urls. It is importent that the protocol matches. If you use both 'https' and 'http' you need to enter both urls.</p>
		</section>
		
  </article>
</div>

